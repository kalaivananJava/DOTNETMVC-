
public class WebDriverCommands{
    public static void main(String[] args) {
        System.out.println("kalaivanan..");
        // WebDriver == browser...it is a interface in java .
        //WebDriver implements many browser drivers classes like => ChromeDriver,GeckoDriver(Firefox),IEDriver(Internet Explorer). 
        //selenium standalone jar files.

    }
}